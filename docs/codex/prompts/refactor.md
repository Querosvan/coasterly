# Refactor Prompt

```text
Refactor this Coasterly area without changing behavior:

<files/modules and reason>

Before editing, inspect existing patterns and identify the smallest safe scope. Preserve public API shapes, seeded fallback behavior, Queue-Times behavior, and current UX.

Do not add abstractions unless they remove real duplication or match an existing local pattern. Do not add dependencies.

Run the relevant existing typecheck/build command for touched workspaces.

When finished, summarize changed files, behavior-preservation notes, validation run, and a suggested Conventional Commit message.
```
